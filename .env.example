# Secrets for local development only — copy to .env.local (gitignored)
#
# NON-sensitive vars are in:
#   .env.development (local dev — committed)
#   .env.production  (Vercel / production — committed)
#
# Next.js auto-loads the right .env file per environment (next dev / vercel deploy).
# Secrets (below) override or supplement committed env files.

ADMIN_SESSION_SECRET=""
WEB_REVALIDATION_SECRET=""
